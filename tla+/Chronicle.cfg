SPECIFICATION Spec


CONSTANTS
    Units                       = {u1, u2, u3, u4}
    Timelines                   = {t1}
    Events                      = {e1, e2}
    TimelineWQ                  = 3
    TimelineAQ                  = 2
    RecordEventRequest          = RecordEventRequest
    RecordEventResponse         = RecordEventResponse
    FetchEventsRequest          = FetchEventsRequest
    FetchEventsResponse         = FetchEventsResponse
    FenceRequest                = FenceRequest
    FenceResponse               = FenceResponse
    Ok                          = Ok
    Unknown                     = Unknown
    InvalidTerm                 = InvalidTerm
    Null                        = Null
    TimelineStatusOpen          = TimelineStatusOpen
    TimelineStatusInRecovery    = TimelineStatusInRecovery
    TimelineStatusClosed        = TimelineStatusClosed
    TimelineTypeBasic           = TimelineTypeBasic
    TimelineTypeCompacted       = TimelineCompacted


SYMMETRY Symmetry


INVARIANT
    TypeOK