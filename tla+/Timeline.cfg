SPECIFICATION Spec

INVARIANT

CONSTANT
    Units = {b1, b2, b3}
    Timelines = {t1}
    Events = {e1, e2}
    TimelineWQ = 3
    TimelineAQ = 2
    Ok = "ok"
    Unknown = "unknown"
    InvalidTerm = "invalid_term"
    Null = "null"
    TimelineStatusOpen = "open"
    TimelineStatusInRecovery = "in_recovery"
    TimelineStatusCanceled = "canceled"
    TimelineBasic = "basic"
    TimelineCompacted = "compacted"

SYMMETRY SymUnits
    SymTimelines
    SymEvents